import { styled } from "@stitches/react";

const StyledContact = styled("section", {
    height: "100vh",
    backgroundColor: "DarkMagenta",
    position: "relative",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  });

  export default function Projects() {
    return (
      <StyledContact>Projects</StyledContact>
    )
  }