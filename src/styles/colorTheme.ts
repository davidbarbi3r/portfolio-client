import {
  teal,
  green,
  sage,
  greenDark,
  tealDark,
  sageDark,
} from "@radix-ui/colors";

export const colorTheme = {
  light: {
    ...teal,
    ...green,
    ...sage,
  },
  dark: {
    ...tealDark,
    ...greenDark,
    ...sageDark,
  },
};
